The SQLiteMigration namespace and SQLite dependencies are intended to be temporary.

When the first PostgreSQL database migration is coded, these components can be removed along with the migration GUI.